import { Router, Route, Link, IndexRoute, hashHistory, browserHistory, createMemoryHistory } from 'react-router';
var myhistory = createMemoryHistory({});
var vrid = "asd";
export = {myhistory,vrid};
